name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r ARCHIVOS/requirements.txt
    - name: Run tests
      run: |
        PYTHONPATH="$GITHUB_WORKSPACE" pytest ARCHIVOS/tests -q

  # Optional: deploy step (commented)
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #   - name: Deploy via SSH
  #     uses: appleboy/ssh-action@v0.1.8
  #     with:
  #       host: ${{ secrets.SERVER_HOST }}
  #       username: ${{ secrets.SERVER_USER }}
  #       key: ${{ secrets.SERVER_SSH_KEY }}
  #       script: |
  #         cd /path/to/project
  #         git pull origin main
  #         source ~/.venvs/docuexpress/bin/activate
  #         pip install -r ARCHIVOS/requirements.txt
  #         sudo systemctl restart gunicorn-docuexpress
